/*
==== Regular Dependencies (use 'npm i'): ====
None

// Initialize storybook in your project dir.
// Creates 'stories' folder at top level but can be moved elsewhere. Basically will pick up any files ending in *.stories.js).
// Also creates .storybook folder which contain config files for the storybook app
npx -p @storybook/cli sb init

// Run the storybook app. Runs at: localhost:6006
npm run storybook
*/
